import 'package:flutter/material.dart';

class ListImageSubBreed extends StatelessWidget {
  const ListImageSubBreed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
